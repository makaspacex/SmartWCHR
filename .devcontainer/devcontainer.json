{
    "name": "humble",
    "image": "makaspacex/smartwchr:dev",
    "customizations": {
        "vscode": {
            "settings": {
                "files.associations": {
                    "*.rviz": "yaml",
                    "*.srdf": "xml",
                    "*.urdf": "xml",
                    "*.xacro": "xml"
                }
            }
        }
    },
    "workspaceFolder": "/ros2_ws",
    "workspaceMount": "source=${localWorkspaceFolder},target=/ros2_ws,type=bind",
    "mounts": [
    ],
    "runArgs": [
        "--net=host",
        // "--device=/dev/bus/usb:/dev/bus/usb",
        // "--device=/dev/ttyUSB1:/dev/imu_usb",
        // "--device=/dev/ttyUSB2:/dev/oradar"
    ],
    // "postCreateCommand": "if [ -f /dev/ttyUSB2 ]; then ln -sf  /dev/ttyUSB2 /dev/oradar; fi"
}
